apiVersion: v1
kind: Service
metadata:
  name: {{ include "autotune.fullname" . }}
  labels:
    {{- include "autotune.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "autotune.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
